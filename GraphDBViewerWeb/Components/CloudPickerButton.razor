@* The Dropbox / OneDrive / Google Drive picker dropdown has been permanently removed. The folder-icon
   button below is a placeholder with no action; its usages (next to the image and 3D-model inputs in
   Home.razor) are currently commented out, so nothing renders. The provider logic still lives in
   wwwroot/js/CloudPickerInterop.js if a picker is ever rebuilt. *@

<button type="button" class="btn btn-outline-secondary btn-sm" title="Browse cloud storage for a file">📁</button>

@code {
    [Parameter] public string Filter { get; set; } = "image";
    [Parameter] public EventCallback<string> OnPicked { get; set; }
}

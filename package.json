{
  "name": "graphdbviewer-jstests",
  "private": true,
  "description": "Browser-JS tests: unit tests for the pure helpers (built-in node:test runner, no dependencies) and Playwright e2e tests for the DOM/canvas layer (needs @playwright/test).",
  "scripts": {
    "test": "node --test \"jstests/**/*.test.js\"",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
